What meets:
1) a stream of tasks (a package of tasks)
2) people — the core
3) people — the cloud

The task is to pick people from the cloud (the shell) to add to the core, so as to:

- cover all the tasks
- minimize the number of conflicts within the core plus shell
